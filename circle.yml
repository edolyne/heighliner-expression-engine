machine:
  node:
    version: 0.12.7

## Customize dependencies
dependencies:
  cache_directories:
    - "node_modules/"
  pre:
    - npm install -g flightplan

deployment:
  hub:
    branch: master
    commands:
      - fly production --harmony
